{
  "name": "@i18n-editor/shared",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "main": "../../dist/shared/index.js",
  "types": "../../dist/shared/index.d.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    },
    "./*": "./src/*"
  },
  "scripts": {
    "build": "yarn clean && esbuild src/index.ts --bundle --outfile=../../dist/shared/index.js --format=esm --minify --sourcemap --platform=node --target=node16 && tsc -p . --emitDeclarationOnly && yarn fix-imports",
    "dev": "concurrently \"tsc -p . --watch\" \"nodemon --watch ../../dist/shared --ext js --exec 'yarn fix-imports'\"",
    "type-check": "tsc -p . --noEmit",
    "fix-imports": "node ../../scripts/fix-esm-imports.js ../../dist/shared",
    "clean": "rm -rf ../../dist/shared"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "nodemon": "^3.1.10"
  }
}
