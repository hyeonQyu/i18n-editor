import { Table } from '@mui/material';
import { TableProps } from 'react-virtuoso';

function NamespaceEditorTable(props: TableProps) {
  return <Table {...props} />;
}

export default NamespaceEditorTable;
